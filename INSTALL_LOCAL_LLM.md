# 🚀 Установка локального DeepSeek через Ollama

## Шаг 1: Установить Ollama

1. Скачайте: https://ollama.com/download/windows
2. Запустите установщик
3. Ollama автоматически запустится как сервис

## Шаг 2: Скачать модель DeepSeek

Откройте PowerShell и выполните:

```powershell
ollama pull deepseek-r1:1.5b
```

**Альтернативы (если мало RAM):**
- `ollama pull qwen2.5:3b` — легкая и быстрая (рекомендую!)
- `ollama pull gemma2:2b` — очень легкая
- `ollama pull llama3.2:3b` — от Meta

**Если есть 16GB+ RAM:**
- `ollama pull deepseek-r1:7b` — лучше качество

## Шаг 3: Проверить работу

```powershell
ollama run deepseek-r1:1.5b "Привет! Как дела?"
```

## Шаг 4: Настроить бота

1. Откройте `.env` и измените:

```env
# СТАРОЕ (закомментируйте или удалите):
# DEEPSEEK_API_KEY=...
# OPENROUTER_MODEL=deepseek/deepseek-chat-v3-0324:free

# НОВОЕ:
USE_LOCAL_LLM=true
LOCAL_LLM_MODEL=deepseek-r1:1.5b
LOCAL_LLM_URL=http://localhost:11434
```

2. Перезапустите бота:

```powershell
py main.py
```

## Готово! 🎉

Теперь бот использует локальную модель без интернета и лимитов!

---

## ⚙️ Дополнительные настройки

### Изменить модель на лету:

```powershell
ollama list  # показать установленные модели
ollama pull qwen2.5:3b  # скачать другую модель
```

Затем в `.env` измените `LOCAL_LLM_MODEL=qwen2.5:3b`

### Удалить модель (освободить место):

```powershell
ollama rm deepseek-r1:1.5b
```

---

## 🆘 Если не работает:

1. Проверьте, запущен ли Ollama:
   ```powershell
   curl http://localhost:11434/api/tags
   ```

2. Перезапустите Ollama:
   - Найдите иконку Ollama в трее
   - ПКМ → Exit
   - Запустите снова из меню Пуск

3. Проверьте логи бота (`bot.log`)

