# Настройка отдельных календарей для мастеров

## ✅ Что сделано

### 1. Созданы файлы конфигурации

**`masters_config.py`** — хранит информацию о мастерах и их календарях:
- Мастер 1: 👩‍🦰
- Мастер 2: 👱‍♀️
- Мастер 3: 👩‍🦳
- Мастер 4: 👩

### 2. Обновлена работа с Google Calendar API

**`calendar_api.py`** — теперь поддерживает:
- `get_free_slots(date, master_id=None)` — получение свободных слотов:
  - Если `master_id` не указан → показывает слоты, где свободен хотя бы один мастер
  - Если `master_id` указан → показывает только слоты этого мастера
  
- `is_slot_free(slot_time, master_id=None)` — проверка занятости:
  - Без `master_id` → проверяет, свободен ли хотя бы один мастер
  - С `master_id` → проверяет только этого мастера
  
- `book_slot(slot_time, client_data, master_id)` — создание записи:
  - **Обязательно** требует `master_id`
  - Создаёт событие в календаре выбранного мастера

### 3. Обновлена логика бота

**`handlers.py`**:
- Бот **обязательно** показывает кнопки выбора мастера перед записью
- Запись **не создаётся** без выбора мастера
- Каждая запись попадает в календарь конкретного мастера

---

## 🔧 Как это работает

### Сценарий бронирования:

1. **Клиент пишет:** "Запишите на стрижку завтра в 15.00"
2. **Бот собирает данные:** имя, телефон, дату, время, услугу
3. **Бот показывает кнопки выбора мастера:**
   ```
   👩‍🦰 Мастер 1    👱‍♀️ Мастер 2
   👩‍🦳 Мастер 3    👩 Мастер 4
   ```
4. **Клиент выбирает мастера**
5. **Бот проверяет:** свободен ли этот мастер в выбранное время
6. **Если свободен** → создаёт запись в календаре мастера
7. **Если занят** → предлагает другое время или другого мастера

---

## 🎯 Преимущества новой архитектуры

✅ **Изоляция записей** — каждый мастер видит только свои записи  
✅ **Гибкость** — можно давать доступ к календарям самим мастерам  
✅ **Масштабируемость** — легко добавить нового мастера (просто создать календарь и добавить в `masters_config.py`)  
✅ **Точность** — нет конфликтов записей между мастерами  

---

## 📝 Как добавить нового мастера

1. **Создайте календарь в Google Calendar** (как для Мастера 1-4)
2. **Дайте доступ сервисному аккаунту** (email из `credentials.json`)
3. **Скопируйте Calendar ID**
4. **Добавьте в `masters_config.py`:**

```python
MASTERS = {
    # ... существующие мастера ...
    "master_5": {
        "id": "master_5",
        "name": "Мастер 5",
        "emoji": "💇",
        "calendar_id": "YOUR_CALENDAR_ID@group.calendar.google.com"
    }
}
```

5. **Перезапустите бота** — всё!

---

## 🐛 Отладка

Логи сохраняются в `bot.log`. Ищите строки:
- `[УСПЕХ] Создано событие в календаре мастера` — запись создана
- `[ОШИБКА] Календарь для мастера ... не найден` — неверный `calendar_id`
- `[ПРЕДУПРЕЖДЕНИЕ] Мастер ... занят` — мастер занят в это время

---

## 📅 Структура календарей

```
Google Account
├── Мастер 1 - НепоседЫ (942b4c9b...)
├── Мастер 2 - НепоседЫ (52b48e66...)
├── Мастер 3 - НепоседЫ (252e7116...)
└── Мастер 4 - НепоседЫ (2f58edcc...)
```

Каждый календарь содержит только записи своего мастера.

---

## ✨ Готово!

Бот теперь работает с **отдельными календарями** для каждого мастера! 🎉

